Небольшое описание перед проверкой:

Что бы отключить автогенерацию данных необходимо закомментировать строку в Program.cs // SeedData.Initialize(app.Services);

Небольшие изменения:

Добавлены отдельные эндпоинты для удаления наблюдателя и исполнителя из задачи, так как объединение всех операций в один метод редактирования сотрудников и данных задачи было бы неудобным.